# Agent Guidance

- Review `README.md` for project-specific instructions before making changes.
- When running project tooling (formatters, linters, tests, etc.), prefix commands with `uv run ...` to ensure the repository's configured tool versions are used.
